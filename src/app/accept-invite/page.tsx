export default function DeprecatedAcceptInvitePage(){
  return <div style={{padding:40,fontFamily:'sans-serif'}}><h2>Fonction invitations retirée</h2><p>Cette page n'est plus active. Utilisez désormais les flux standards via <code>/auth/accept</code>.</p><a href="/auth/accept">Aller à /auth/accept</a></div>;
}
